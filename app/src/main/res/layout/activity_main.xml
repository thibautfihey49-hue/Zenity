<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#121212">

    <!-- Barre d'URL -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:orientation="horizontal"
        android:background="#1E1E1E"
        android:paddingStart="4dp"
        android:paddingEnd="4dp">

        <EditText
            android:id="@+id/urlBar"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:hint="URL ou recherche…"
            android:textColor="#FFFFFF"
            android:textColorHint="#888888"
            android:background="@android:color/transparent"
            android:inputType="textUri"
            android:imeOptions="actionGo"
            android:singleLine="true"
            android:padding="8dp"
            android:textSize="14sp"/>

        <Button
            android:id="@+id/goButton"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:text="Go"
            android:textSize="12sp"
            style="@style/Widget.MaterialComponents.Button.TextButton"/>
    </LinearLayout>

    <!-- Boutons d'actions -->
    <HorizontalScrollView
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:background="#1A1A1A"
        android:scrollbars="none">

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:orientation="horizontal">

            <Button android:id="@+id/tabNew" android:layout_width="wrap_content" android:layout_height="match_parent" android:text="Onglet" android:textSize="11sp" style="@style/Widget.MaterialComponents.Button.TextButton"/>
            <Button android:id="@+id/videoDownload" android:layout_width="wrap_content" android:layout_height="match_parent" android:text="DL Vidéo" android:textSize="11sp" style="@style/Widget.MaterialComponents.Button.TextButton"/>
            <Button android:id="@+id/addFav" android:layout_width="wrap_content" android:layout_height="match_parent" android:text="Favori" android:textSize="11sp" style="@style/Widget.MaterialComponents.Button.TextButton"/>
            <Button android:id="@+id/ghostMode" android:layout_width="wrap_content" android:layout_height="match_parent" android:text="Ghost" android:textSize="11sp" style="@style/Widget.MaterialComponents.Button.TextButton"/>
            <Button android:id="@+id/turboMode" android:layout_width="wrap_content" android:layout_height="match_parent" android:text="Turbo" android:textSize="11sp" style="@style/Widget.MaterialComponents.Button.TextButton"/>
            <Button android:id="@+id/ultraMode" android:layout_width="wrap_content" android:layout_height="match_parent" android:text="Ultra" android:textSize="11sp" style="@style/Widget.MaterialComponents.Button.TextButton"/>
            <Button android:id="@+id/quantumMode" android:layout_width="wrap_content" android:layout_height="match_parent" android:text="Quantum" android:textSize="11sp" style="@style/Widget.MaterialComponents.Button.TextButton"/>
        </LinearLayout>
    </HorizontalScrollView>

    <!-- WebView -->
    <WebView
        android:id="@+id/webView"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"/>
</LinearLayout>
